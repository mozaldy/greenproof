/// Task Registry — stores task IDs, status, and claim records on-chain.
/// Metadata (photos, diagnosis, GPS) stays in Postgres; only IDs and balances on-chain.
/// See TechSpec Section 4.1.
module greenproof::task_registry {
    // TODO: implement publish_task, claim_task, confirm_submission
    // See TechSpec Section 4.1 for full interface spec
}
