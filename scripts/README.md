# Scripts

CLI helpers untuk operasional GreenProof. Dijalankan langsung via `tsx`.

Placeholder untuk:
- `deploy-contracts.ts` — `sui client publish` wrapper
- `seed-db.ts` — seed data demo untuk hackathon
- `create-company.ts` — onboard klien baru (Admin GreenProof task)
- `set-validator-level.ts` — set level mitra manual (sebelum auto-leveling diimplementasi)
